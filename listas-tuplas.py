cursos = ['Python', 'Django', 'Flask' ] # Lista

# Generar Tuplas a partir de listas
cursos_tuple = tuple(cursos)
print(cursos_tuple)
print(type(cursos_tuple))

niveles = ('Básico', 'Intermedio', 'Avanzado') # Tupla

# Generar listas a partir de tuplas
niveles_list = list(niveles)
print(niveles_list)
print(type(niveles_list))